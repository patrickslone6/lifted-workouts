name: Fix refresh persistence
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { ref: main, fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - name: Apply and build
        run: |
          node scripts/fix-refresh-persistence.mjs
          npm install
          npm run build
      - name: Commit and clean up
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f scripts/fix-refresh-persistence.mjs .github/workflows/fix-refresh-persistence.yml
          git add -A
          git commit -m "Restore completed training after refresh"
          git push origin main
